Run ESLint on the project. Fix any errors in files that were modified during this session.

```bash
npm run lint
```

If linting fails:
1. Read the error output to identify which files and lines have issues
2. Only fix errors in files that were modified during this conversation — do not fix pre-existing lint errors in unrelated files
3. Re-run `npm run lint` to confirm the fix
